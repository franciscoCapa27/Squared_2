# Vertex Progression Tree

The Vertex Shop will become a fixed, authored, scrollable progression tree while preserving the existing upgrade IDs, costs, effects, and prerequisite mechanics. The root is a mandatory purchasable gateway; only the root is initially visible, purchasing a node progressively reveals its direct children, and nodes show state through minimalist Godot-native icons, connections, and a focused detail popup rather than text-heavy cards.
